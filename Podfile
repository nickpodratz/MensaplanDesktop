# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MensaplanDesktop' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MensaplanDesktop
  pod 'Moya', '8.0.0-beta.5'
  pod 'MoyaSugar', '~> 0.2'
  pod 'SwiftyJSON'

  target 'MensaplanDesktopTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'MensaplanTodayExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Moya', '8.0.0-beta.5'
  pod 'MoyaSugar', '~> 0.2'
  pod 'SwiftyJSON'

  # Pods for MensaplanTodayExtension

end
